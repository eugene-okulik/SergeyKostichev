from enum import Enum


class Messages(Enum):
    EXISTED_EMAIL_ERROR = "Please enter the same value again."
    SUCCESSFUL_REGISTRATION = "Thank you for registering with Main Website Store."
    UNSUCCESSFUL_REGISTRATION = "Invalid answer when all fields are True"